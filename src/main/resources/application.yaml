spring:
  application:
    name: smoke-weather-api
  mvc:
    servlet:
      path: /api/v1
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: smoke-weather
      auto-index-creation: true
  jackson:
    deserialization:
      fail-on-unknown-properties: true

server:
  port: 8000